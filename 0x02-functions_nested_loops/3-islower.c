#include "main.h"
/**
  * _islower -  a function that checks for lowercase character.
  * return 1 success
  * return 0 failed
  */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
