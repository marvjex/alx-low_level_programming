#include "mul.h"
/**
 * mul - multiplies two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: The product of the two integers.
 */
int mul(int a, int b)

{
int mul(int a, int b)
return (a * b);
}
