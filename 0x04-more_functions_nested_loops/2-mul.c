#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: the product of a and b
 */
int mul(int a, int b)

{
int result = 0;
int i;

for
(i = 0);
(i < b);
(i++);
{
result += a;
}
return (result);
}
